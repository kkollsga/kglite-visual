# Pinned to the same floors the sibling KGLite docs build against, minus
# sphinx-autoapi: this project's Python surface is two names and its reference
# page is hand-written (see the comment in conf.py).
sphinx>=9.1.0
furo>=2025.12.19
myst-parser>=5.1.0
sphinx-copybutton>=0.5.2
